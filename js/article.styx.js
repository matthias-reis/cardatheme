sx.build({
    name: 'Article',
    nsp: 'carda',
    parent: sx.Plugin,

    m: {
        init: function () {
//            this.el.find('.sharedaddy').clone().prependTo(this.socialTarget);
        }
    }
});